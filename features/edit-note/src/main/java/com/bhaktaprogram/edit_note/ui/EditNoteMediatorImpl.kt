package com.bhaktaprogram.edit_note.ui

import androidx.fragment.app.FragmentManager
import com.bhaktaprogram.coreapi.navigation.EditNoteMediator
import javax.inject.Inject

class EditNoteMediatorImpl @Inject constructor() : EditNoteMediator {

    override fun startEditNotes(containerId: Int, fragmentManager: FragmentManager, noteId: Int) {
        TODO("Not yet implemented")
    }
}